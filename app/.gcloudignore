# Keep the Cloud Build upload lean and let the Docker image build its own deps.
# (Without this, a local node_modules/dist would be uploaded and COPY . . would
# clobber the image's clean `npm ci` output.)
.git/
node_modules/
dist/
*.log
.DS_Store
